apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongo-charts.fullname" . }}-test-connection"
  labels:
    {{- include "mongo-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongo-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
